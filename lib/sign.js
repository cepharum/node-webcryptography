/**
 * @param { AlgorithmIdentifier } algorithm
 * @param { CryptoKey } key
 * @param { BufferSource } data
 * @return Promise <any>
 */
function sign( algorithm, key, data ) {
	// Todo
}

module.exports = sign;